{
  "version": 2,
  "builds": [
    {
      "src": "apps/landing/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/apps/landing/dist/$1"
    }
  ]
}
