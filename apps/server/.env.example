# Server Configuration
PORT=3000
NODE_ENV=development

# Client URL (for CORS)
CLIENT_URL=http://localhost:5173

# Database
DATABASE_URL="postgresql://genericcorp:genericcorp@localhost:5432/genericcorp?schema=public"

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Encryption key for storing OAuth tokens at rest (AES-256-GCM)
# Generate with: openssl rand -base64 32
# REQUIRED for provider account features
ENCRYPTION_KEY=

# Note: Claude Agent SDK auto-detects credentials from ~/.claude/.credentials.json
# No ANTHROPIC_API_KEY needed if using Claude Code
