// Re-export from core — app events are now in @generic-corp/core
export { appEventBus } from "@generic-corp/core";
export type { AppEventMap, CoreEventMap, TenantEventBase } from "@generic-corp/core";
